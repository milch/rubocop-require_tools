RSpec.describe Rubocop::RequireTools do
  it "has a version number" do
    expect(Rubocop::RequireTools::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
